


mod test{
    use super::*;

    struct tester{
        x,
        y
    }

    #[test]
    fn testing_testing_123(){
        let tstr = {
            x:0,
            y:3
        };

        assert!(tstr.x > tstr.y);
        assert!(x < 3);
    }

}